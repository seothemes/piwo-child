 $(".grdnt-btn").on("click", function(e) { // When btn is pressed.
    console.log('magicalData.siteURL');
          // e.preventDefault();
          // var catTipo = $(this).data('slug');
            
          // $(".grdnt-btn").removeClass("active");
        
          // $(".cont_query").hide();
  
          // $(".cont-post_" + catTipo ).fadeIn(400);
          
          // $(this).addClass("active");

          // var ourRequest = new XMLHttpRequest(); 
          //     ourRequest.open('GET', magicalData.siteURL + '/wp-json/wp/v2/posts?categories=6&order=asc');     ourRequest.onload = function() {   
          //           if (ourRequest.status >= 200 && ourRequest.status < 400) {  
          //   var data = JSON.parse(ourRequest.responseText);  
          //   createHTML(data);
          //   portfolioPostsBtn.remove();
          //        } else {  
          //           console.log("We connected to the server, but it returned an error.");       } 
          //          };
          
  
    });