<label class="radio-wrapper text--bold" for="{{ id }}">
	<input class="form__input input--radio {{ input_modifier }}" id="{{ id }}" name="{{ name }}" type="radio" value="{{ value }}" />

	{{ text }}
</label>